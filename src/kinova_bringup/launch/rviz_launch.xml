<launch>
  <let name="urdf_path" value="$(find-pkg-share kinova_description)/urdf/robot.urdf.xacro" />
  <let name="controllers_path" value="$(find-pkg-share kinova_bringup)/config/controllers.yaml" />

  <!-- 1. Publishes the 3D geometry transforms -->
  <node pkg="robot_state_publisher" exec="robot_state_publisher">
    <param name="robot_description" value="$(command 'xacro $(var urdf_path)')" />
  </node>

 
  <node pkg="controller_manager" exec="ros2_control_node">
    <param name="robot_description" value="$(command 'xacro $(var urdf_path)')" />
    <param from="$(var controllers_path)" />
  </node>

  <!-- <node pkg = "joint_state_publisher_gui" exec = "joint_state_publisher_gui" />  -->

 
  <node pkg="controller_manager" exec="spawner" args="joint_state_broadcaster" />
  <node pkg="controller_manager" exec="spawner" args="joint_position_controller" />
  <node pkg="controller_manager" exec="spawner" args="cartesian_motion_controller --inactive" />

  <!-- 4. Visualization -->
  <node pkg="rviz2" exec="rviz2" output="screen" />
</launch>